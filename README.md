# ch8_repo

This repository contains the ASP.NET Core MVC project for my Chapter 8 assignment.  
It was created using the `dotnet new mvc` command and is used to practice Git and GitHub version control workflows in Visual Studio Code.

## How to Run

1. Open the project folder in VS Code.  
2. In the terminal, run:
   ```bash
   dotnet run